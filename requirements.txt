# Adaptive_Model_Selection — pip dependencies (Python 3.10+ recommended).
#
# PyTorch: install a build that matches your OS/GPU (CPU vs CUDA) from
#   https://pytorch.org/get-started/locally/
# then install this file:
#   pip install -r requirements.txt
#
# If torch-geometric wheels fail to resolve, install PyTorch first, then PyG using the
# wheel index matching your torch version: https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html

# Core numerics & plotting
numpy>=1.24
matplotlib>=3.7
Pillow>=10.0
fastdtw>=0.3.4
pandas>=2.0
plotly>=5.18

# ML / GNN (Training, Inference, modules/gradient_smoothing, etc.)
torch>=2.0.0
torch-geometric>=2.4.0
networkx>=3.0
scikit-learn>=1.3.0
joblib>=1.3
optuna>=3.0.0
tqdm>=4.65.0

# Config & environment
pyyaml>=6.0
python-dotenv>=1.0.0
dropbox>=11.36.0
deepl>=1.0.0

# Temporary/plot_avg_decrease_vs_epsilon.py (TikZ export); safe to omit if unused
tikzplotlib>=0.10.0

# Optional: Jupyter notebooks under Training/ (uncomment if needed)
# jupyter>=1.0.0
# ipykernel>=6.0.0
